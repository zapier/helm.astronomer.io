################################
## Airflow Secrets
#################################
{{- if not .Values.fernetKeySecretName }}
{{ $generated_fernet_key := (randAlphaNum 32 | b64enc) }}
kind: Secret
apiVersion: v1
metadata:
  name: {{ .Release.Name }}-fernet-key
  namespace: {{ include "namespace" . }}
  labels:
    release: {{ .Release.Name }}
    chart: {{ .Chart.Name }}
    heritage: {{ .Release.Service }}
type: Opaque
data:
  fernet-key: {{ (default $generated_fernet_key .Values.fernetKey) | b64enc | quote }}
{{- end }}
---
# If both of these secret names are not set, we will either use the set password, or generate one
{{- if not (and .Values.redis.passwordSecretName .Values.redis.brokerURLSecretName) }}
{{ $random_redis_password := randAlphaNum 10 }}
kind: Secret
apiVersion: v1
metadata:
  name: {{ .Release.Name }}-redis-password
  namespace: {{ include "namespace" . }}
  labels:
    release: {{ .Release.Name }}
    chart: {{ .Chart.Name }}
    heritage: {{ .Release.Service }}
type: Opaque
data:
  password: {{ (default $random_redis_password .Values.redis.password) | b64enc | quote }}
---
kind: Secret
apiVersion: v1
metadata:
  name: {{ .Release.Name }}-broker-url
  namespace: {{ include "namespace" . }}
  labels:
    release: {{ .Release.Name }}
    chart: {{ .Chart.Name }}
    heritage: {{ .Release.Service }}
type: Opaque
data:
  connection: {{ (printf "redis://:%s@%s-redis:6379/0" (default $random_redis_password .Values.redis.password) .Release.Name) | b64enc | quote }}
{{- end }}
---
{{- if (and .Values.data.metadataConnectionString (not .Values.data.metadataSecretName)) }}
kind: Secret
apiVersion: v1
metadata:
  name: {{ .Release.Name }}-airflow-metadata
  namespace: {{ include "namespace" . }}
  labels:
    release: {{ .Release.Name }}
    chart: {{ .Chart.Name }}
    heritage: {{ .Release.Service }}
type: Opaque
data:
  connection: {{ .Values.data.metadataConnectionString | b64enc | quote }}
{{- end }}
---
{{- if (and .Values.data.resultBackendConnectionString (not .Values.data.resultBackendSecretName)) }}
kind: Secret
apiVersion: v1
metadata:
  name: {{ .Release.Name }}-airflow-result-backend
  namespace: {{ include "namespace" . }}
  labels:
    release: {{ .Release.Name }}
    chart: {{ .Chart.Name }}
    heritage: {{ .Release.Service }}
type: Opaque
data:
  connection: {{ .Values.data.resultBackendConnectionString | b64enc | quote }}
{{- end }}
